# capturar-direto

Projeto com um servidor Node.js automatizado em `aviator-bot/` para login no MegaGameLive, captura contínua de multiplicadores do Aviator e exposição dos dados via API REST.

O repositório também inclui `render.yaml` para deploy direto na Render usando Blueprint.
